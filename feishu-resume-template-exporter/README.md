# 飞书简历模板导出

你已经在飞书里写好了简历，但投递前还要反复排 Word、导 PDF，而且一导出就乱版。这个 Skill 的作用，是把飞书文档里的结构化内容直接做成一份按固定样稿排版的 Word 和 PDF。

它不是把飞书文档“另存为 Word”。它会以样稿为版式标准，重新排出身份信息、头像、分区标题、经历条目、日期和项目符号，让最终文件可以直接用于投递。

## 你会拿到什么

- 与样稿同一套版式的 `.docx`；
- 检测到 Microsoft Word 时由 Word 自动导出的 PDF；未检测到时由 LibreOffice 生成的兼容 PDF；
- 顶部姓名、联系方式和头像的协调布局；
- 左侧公司/项目与角色、右侧日期的简历条目；
- 一页放得下就保持单页，内容更多时自然延展，避免把文字压得不可读。

## 适合怎样的飞书文档

文档至少应包含：姓名与联系方式、个人总结、工作经历、教育经历、技能。经历建议按下面的结构填写：

```text
公司/项目｜角色　时间
一句职责概述
- 关键动作、判断和结果
- 下一条经历或成果
```

没有的经历可以不写。不要为了填满模板编造公司、职责、数字或结果。

## 怎么开始

把本页面链接发给你正在使用的 Agent：

```text
请帮我安装这个 Skill：
https://github.com/Luyu2026/Skill-Bible/tree/main/feishu-resume-template-exporter

我只想安装这一个 Skill，不需要下载整个仓库。
请你自己判断我当前使用的 Agent、正确的 Skill 安装目录和下载方式，并直接完成安装。
安装完成后告诉我：装在了哪里，以及我下一句该怎么调用它。
```

安装后，把你的飞书简历链接和下面这句话发给 Agent：

```text
请使用「飞书简历模板导出」Skill，把这份飞书文档按内置简历样稿生成可投递的 Word 和 PDF：

【飞书文档链接】

头像：我已提供 / 暂未提供。
不要编造任何经历、数据或结果；如缺少头像，请在演示文件中使用明确标注的模拟头像，不要把它当作我的真实照片。
```

## 这个 Skill 的边界

它负责排版和导出，不负责替你补经历或虚构事实。导出时会先检查本机的软件：有 Microsoft Word 时直接自动调用 Word 导出 Word 与 PDF；没有 Word 时，Skill 会同时交付 Word 和 LibreOffice 生成的兼容 PDF。Skill 已自带中文字体，避免兼容导出时出现文字方框。若你的电脑有 WPS，也可以用收到的 Word 文件在 WPS 中自行导出 PDF，以本机 WPS 的排版结果为准。飞书导出和重新绘制 PDF 都不会被当作 Word 保真的方案。简历内容还没有写好时，建议先使用“JD 定制简历”或“简历去 AI 味”，再用这个 Skill 输出最终投递文件。
